#include "wrap_event.h"

void
event_server(int fd) {
  // struct event_config *cfg;
  // struct event_base *base;
  // struct event *listen_event;

  // base = event_base_new();
  // if (!base) {
  //   perror("event");
  //   exit(EXIT_FAILURE);
  // }
}
